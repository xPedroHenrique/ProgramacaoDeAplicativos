// formulario para CRUD de usuario

class FormUser {
    constructor(login, password){
        this.login = login;
        this.password = password;
    }
}

module.exports = {
    FormUser
}